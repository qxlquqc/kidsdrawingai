/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  experimental: {},
  env: {},
  images: {
    domains: ['replicate.delivery'],
  },
};

module.exports = nextConfig; 