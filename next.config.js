/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  experimental: {},
  env: {},
  images: {
    domains: [
      'replicate.delivery',
      'lh3.googleusercontent.com'
    ],
  },
};

module.exports = nextConfig; 